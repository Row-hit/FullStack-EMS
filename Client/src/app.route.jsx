import { createBrowserRouter } from "react-router-dom";

const router = createBrowserRouter([
  {
    path: "/",
    element: <div className="text-2xl bg-red-500">HEllooo World</div>,
  },
]);

export default router;
